GCD function (a, b)
while b ≠ 0
t: = b
b: = a mod b
a: = t
returns a


GCD function (a, b)
     while ≠ b
         if a> b
             a: = a - b
         other
             b: = b - a
     returns a


GCD function (a, b)
     if b = 0
         returns a
     other
         return gcd (b, a mod b)